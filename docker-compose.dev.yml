services:
  feed-notifier:
    container_name: feed-notifier
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./app:/app
      - ./data:/data
    environment: 
      PYTHONUNBUFFERED: "1" 
    command: python main.py